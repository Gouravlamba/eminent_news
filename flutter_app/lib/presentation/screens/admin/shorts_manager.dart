import 'package:flutter/material.dart';

class ShortsManager extends StatelessWidget {
  const ShortsManager({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Admin - Shorts Manager'),
      ),
      body: const Center(
        child: Text('Shorts Manager - Work in Progress'),
      ),
    );
  }
}
