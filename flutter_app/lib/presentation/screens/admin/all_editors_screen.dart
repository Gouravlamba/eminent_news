import 'package:flutter/material.dart';

class AllEditorsScreen extends StatelessWidget {
  const AllEditorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Admin - All Editors'),
      ),
      body: const Center(
        child: Text('All Editors Screen - Work in Progress'),
      ),
    );
  }
}
