import 'package:flutter/material.dart';

class EditorShorts extends StatelessWidget {
  const EditorShorts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Editor - My Shorts'),
      ),
      body: const Center(
        child: Text('Editor Shorts - Work in Progress'),
      ),
    );
  }
}
